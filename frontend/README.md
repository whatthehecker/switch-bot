# Switch Bot Frontend

Frontend application for the Websocket-based SwitchBot.

## Generating required code
Some Dart code is automatically generated and not checked into Git.
To build these files, use
```shell
dart run build_runner build
```
