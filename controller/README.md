# Controller firmware
The controller is the software running on Pro Micro (or another ATmega32u4-based MCU) that acts as a game controller attached by USB to the Nintendo Switch console.

It takes in commands through its UART and translates them into actions the simulated controller executes.